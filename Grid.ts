class Grid{
    #numRow: number;
    #numCol: number;

    constructor(row: number, column: number, element: HTMLDivElement){
        this.#numRow = row;
        this.#numCol = column;
    }

    // display(){}
    // moveTiles(){}
    // dropRandomNumber(){}
    // clear(){}
}

